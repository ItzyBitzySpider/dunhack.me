import { PrismaClient } from '@prisma/client';

const prisma = new PrismaClient();

//init DB with default values

//load env config into DB

//force reload of DB config to map


//export DB config as a map for fast lookup